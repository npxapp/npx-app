// TallerBox.js
import React from 'react';
import { Box } from '@mui/material';

const TallerBox = () => {
  return (
    <Box sx={{ padding: 2, }}>
      <p>Single Point of Entry, Express Middleware Server, Router.js for Single Page Application Routing, Path Mapping, Event Listeners Like PopState, Handling Link Clicks, Updating Page Content, Loading Page Components, Waiting for Static Assets, Component Initialization</p>
    </Box>
  );
};

export default TallerBox;